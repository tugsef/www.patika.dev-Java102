package shoppingStore.patikaStore;

import java.util.List;

import shoppingStore.ShoppingStore;

public interface PatikaStore extends ShoppingStore{

	PatikaStore add(PatikaStore s);
	void delete(int x);
	void showInfo(List<PatikaStore> s);
	
}
